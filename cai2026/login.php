<?php
require_once __DIR__.'/config/bootstrap.php';if(user()){header('Location: '.base_url('admin/index.php'));exit;}$error='';
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$login=strtolower(trim((string)$_POST['email']));if($login==='admin')$login='admin@cai2026.com.ar';$q=db()->prepare('SELECT * FROM usuarios WHERE email=? AND activo=1');$q->execute([$login]);$u=$q->fetch();if($u&&$u['password_hash']&&password_verify((string)$_POST['password'],$u['password_hash'])){session_regenerate_id(true);$_SESSION['user']=['id'=>(int)$u['id'],'nombre'=>$u['nombre'],'email'=>$u['email'],'rol'=>$u['rol']];db()->prepare('UPDATE usuarios SET ultimo_acceso=NOW() WHERE id=?')->execute([$u['id']]);audit('login','usuarios',(int)$u['id']);header('Location: '.base_url('admin/index.php'));exit;}$error='Usuario o contraseña incorrectos.';}
?><!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Ingreso CAI 2026</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="<?=base_url('assets/css/app.css')?>" rel="stylesheet"></head><body class="login-page"><main class="login-card"><a href="<?=base_url()?>" class="brand"><span class="brand-mark">CAI</span><span>Competencia Andina Invernal <b>2026</b></span></a><h1>Panel de competencia</h1><p class="muted">Acceso exclusivo para autoridades y jueces.</p><?php if($error):?><div class="alert alert-danger"><?=h($error)?></div><?php endif?><form method="post"><?=csrf_input()?><label>Usuario o correo</label><input class="form-control" type="text" name="email" required autofocus><label>Contraseña</label><input class="form-control" type="password" name="password" required><button class="btn btn-primary w-100">Ingresar</button></form><a href="<?=base_url()?>" class="back-link">← Volver al tablero público</a></main></body></html>
